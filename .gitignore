# IDE / editor
.idea/
.vscode/
.DS_Store
.cursor/

# Node / Python / build artifacts (defensive)
node_modules/
dist/
build/
*.log

# Docker / volumes
local-dev/pgdata/
windows/db_initialized.flag

# Archives and backups
archive/
ppa-*.dump

# Installer binaries
windows/ppa-wizard-setup*.exe
windows/data_seeded.flag

# Local S3 data (large/generated)
local-dev/s3/datasource/
local-dev/s3/output/

# Original PPA source drop (contains certs/keys; keep local only)
PPA sourcecode/

# AWS infra repo (keep local, not tracked)
ppa-infra/

